/*
 * This configuration extends the default @grafana/create-plugin webpack config.
 * Custom overrides are applied in the root webpack.config.ts via webpack-merge.
 */
import config from '../../webpack.config';

export default config;
